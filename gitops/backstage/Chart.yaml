apiVersion: v2
name: backstage-umbrella
description: Umbrella chart for deploying Backstage through ArgoCD GitOps
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: backstage
    version: "~1.9.0"
    repository: "https://backstage.github.io/charts"

